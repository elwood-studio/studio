{
  "name": "@elwood/service-workflow",
  "version": "0.1.13",
  "type": "commonjs",
  "scripts": {
    "dev": "pnpm run build --watch",
    "build": "esbuild entry.ts --format=cjs --bundle --sourcemap --platform=node --target=node18 --outdir=./dist \"--external:ssh2\" \"--external:./transpilers/swc.js\""
  },
  "devDependencies": {
    "@elwood/workflow-service": "workspace:*",
    "esbuild": "^0.18.6",
    "tsconfig": "workspace:*",
    "typescript": "^5.1.3"
  }
}
