{
  "name": "@elwood-studio/service-workflow",
  "version": "0.1.11",
  "type": "commonjs",
  "scripts": {
    "build": "esbuild entry.ts --format=cjs --bundle --sourcemap --platform=node --target=node18 --outdir=./dist \"--external:ssh2\" \"--external:./transpilers/swc.js\""
  },
  "devDependencies": {
    "@elwood-studio/workflow-service": "workspace:*",
    "esbuild": "^0.18.6",
    "tsconfig": "workspace:*",
    "typescript": "^5.1.3"
  }
}
