{
  "name": "@elwood/service-workflow",
  "private": true,
  "version": "0.1.17",
  "type": "commonjs",
  "scripts": {
    "dev": "pnpm run build --watch",
    "build": "esbuild entry.ts --format=cjs --bundle --sourcemap --platform=node --target=node18 --outdir=./dist"
  },
  "devDependencies": {
    "@elwood/workflow-service": "workspace:*",
    "esbuild": "^0.18.9",
    "tsconfig": "workspace:*",
    "typescript": "^5.1.3"
  }
}
